<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thietbidientu_fontend">

    <!-- Quyền mạng: tải API/ảnh qua HTTP/HTTPS -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Tùy nhu cầu thực tế của app -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- READ/WRITE_EXTERNAL_STORAGE cũ, chỉ hữu dụng với targetSdk < 33 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Nếu targetSdk 33+ và có cần đọc ảnh, cân nhắc dùng quyền mới:
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    -->

    <application
        android:name="${applicationName}"
        android:label="thietbidientu_fontend"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Flutter embedding v2 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Cho phép truy vấn implicit intent dùng PROCESS_TEXT (tùy chọn) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
